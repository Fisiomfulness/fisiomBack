const { getAllInterests } = require('./getAllInterests');
const { createInterest } = require('./createInterest');
const { deleteInterest } = require('./deleteInterest');

module.exports = {
  getAllInterests,
  createInterest,
  deleteInterest,
};
